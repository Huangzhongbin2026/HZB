# 系统管理模块架构与流程

## 1. 模块融合方案
系统管理模块作为平台基础服务，独立为 supply-system-service，向其他服务输出统一能力：
- 菜单与按钮权限服务。
- 角色、数据权限、字段权限服务。
- 用户与账号安全服务。
- 数据字典全局服务。
- 操作日志采集与检索服务。

## 2. 权限校验流程图
```mermaid
flowchart LR
    A[用户登录] --> B[校验账号状态/密码]
    B --> C[加载用户角色]
    C --> D[聚合菜单权限]
    C --> E[聚合按钮权限]
    C --> F[聚合数据范围权限]
    C --> G[聚合字段权限]
    D --> H[前端动态渲染菜单]
    E --> I[前端v-permission按钮控制]
    F --> J[后端数据权限注解拦截]
    G --> K[后端字段过滤器处理]
    H --> L[页面访问请求]
    I --> L
    L --> M[网关鉴权]
    M --> N[接口权限校验]
    N --> O[返回数据]
    O --> P[前端字段可见/可编辑判断]
```

## 3. 系统日志记录流程图
```mermaid
sequenceDiagram
    participant U as 用户
    participant FE as 前端
    participant GW as 网关
    participant S as 系统管理服务
    participant AOP as SysLog切面
    participant DB as MySQL

    U->>FE: 触发操作
    FE->>GW: 请求API
    GW->>S: 路由并鉴权
    S->>AOP: 命中@SysLog注解
    AOP->>S: 执行业务方法
    S->>DB: 落业务数据
    AOP->>DB: 异步落操作日志
    S-->>FE: 返回结果
    FE->>S: 上报前端埋点日志(可选)
```

## 4. 核心表关联关系图
```mermaid
erDiagram
    sys_user ||--o{ sys_user_role : has
    sys_role ||--o{ sys_user_role : assigned
    sys_role ||--o{ sys_role_menu : grants
    sys_menu ||--o{ sys_role_menu : bound
    sys_role ||--o{ sys_role_data_permission : controls
    sys_role ||--o{ sys_role_field_permission : controls
    sys_dict_type ||--o{ sys_dict_item : contains
    sys_user ||--o{ sys_operation_log : operates
```

## 5. 设计说明
- 多角色权限叠加：菜单/按钮权限取并集，数据范围权限取最宽集合策略，字段权限按优先级合并（可编辑 > 可见 > 不可见）。
- 字段级权限实现：在 Service 层返回前通过字段过滤器对 VO 字段进行裁剪；查询层配合 SQL 拦截器减少无效字段读取。
- 日志性能：核心操作日志异步写入，查询按时间与模块建立复合索引。
- 缓存策略：用户权限缓存 30 分钟，角色变更后支持主动刷新缓存。
